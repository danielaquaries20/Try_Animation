package com.example.tryanimation.try_architecture_code.data.const

class Constants {
    object TOKEN {
        const val API_TOKEN = "api_token"
        const val API_TOKEN_EXPIRED = "api_token_expired"
        const val FCM_TOKEN = "fcm_token"
    }
}